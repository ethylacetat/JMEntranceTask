package ru.ethylacetat.entrancetask.interfaces;

public interface Calculator {
    Number add(Number one, Number two);
    Number subtract(Number one, Number two);
    Number multiply(Number one, Number two);
    Number divide(Number one, Number two);
}
